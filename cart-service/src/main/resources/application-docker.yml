spring:
  data:
    mongodb:
      uri: mongodb://mongouser:mongopassword@Cart-Service:27018/crm?authSource=admin
eureka:
  client:
    service-url:
      defaultZone: "http://discovery-server-app:8888/eureka/"
    register-with-eureka: true
    fetch-registry: true
