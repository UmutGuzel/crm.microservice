spring:
  datasource:
    url: "jdbc:postgresql://localhost:5434/crm"
    username: myuser
    password: mypassword
eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8888/eureka/"
    register-with-eureka: true
    fetch-registry: true
